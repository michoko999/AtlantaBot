name: Build and Publish Docker Image

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */6 * * *"

jobs:
  build:
    runs-on: ubuntu-latest
    environment: docker

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout AtlantaBot upstream
        uses: actions/checkout@v4
        with:
          repository: Androz2091/AtlantaBot
          ref: master

      - name: Prepare build context
        run: |
          echo -e "node_modules\n.git\n.github" > .dockerignore

          if [ ! -f package-lock.json ]; then
            npm install --package-lock-only --ignore-scripts
          fi

          node -e '
            const fs = require("fs");
            let df = fs.readFileSync("Dockerfile", "utf8");

            df = df.replace(
              "RUN npm ci",
              "RUN apt-get update && apt-get install -y --no-install-recommends python3 python-is-python3 build-essential && rm -rf /var/lib/apt/lists/*\nRUN npm ci"
            );

            df = df.replace(
              "RUN npm ci --omit=dev",
              "COPY --from=builder /app/node_modules ./node_modules"
            );

            fs.writeFileSync("Dockerfile", df);
          '

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ secrets.DOCKERHUB_USERNAME }}/atlantabot
            ghcr.io/${{ github.repository_owner }}/atlantabot
          tags: |
            type=raw,value=latest
            type=sha,prefix=sha-

      # Émulateur requis pour construire l'image ARM64 sur les serveurs x86 de GitHub
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Build multi-plateforme : PC et Raspberry Pi
      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
